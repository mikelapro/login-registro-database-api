//#region Usecase

/**
 * Obtiene información del API.
 * @returns La información del API.
 */
const getApiInfo = () => {
    const apiInfo = { about: 'Login registro database api API', by: 'Mike' };

    return apiInfo;
};

//#endregion

module.exports = {
    getApiInfo
};